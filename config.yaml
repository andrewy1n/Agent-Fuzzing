fuzzer:
  time_limit: 300
  execution_limit: 0
  per_run_timeout: 2
  steps_per_seed: 5
  mutations_per_step: 5
  state_address_offset: 0
  state_reg: []

target:
  binary: ''
  rootfs: 'rootfs/x8664_linux_glibc2.39'

input:
  method: 'stdin'
  seed_inputs: []

output:
  dir: 'out'

mutation_agent:
  model: 'gpt-4.1'
  prompt: ''
